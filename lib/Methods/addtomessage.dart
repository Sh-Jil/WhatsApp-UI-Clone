import 'package:flutter/material.dart';

class AddToMessage extends StatelessWidget {
  final String message;
  const AddToMessage({Key? key, required this.message}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
